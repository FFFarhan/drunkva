window.DRUNKVA_CONFIG = {
    // For Render static frontend, set this to your Render backend URL:
    // Example: "https://drunkva-api.onrender.com"
    // Leave empty to use same-origin (or localhost fallback when opened via file://).
    API_BASE_URL: ""
};
